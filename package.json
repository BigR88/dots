{
  "name": "dots",
  "version": "0.1.0",
  "private": true,
  "description": "dots — Event-Discovery-App für Frankfurt am Main",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "mobile": "npm run start -w apps/mobile",
    "mobile:web": "npm run web -w apps/mobile",
    "typecheck": "npm run typecheck --workspaces --if-present"
  }
}
